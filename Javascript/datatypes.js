"use strict"; // treat all js code as new version

// alert("Hello world")  we are using node.js ,not browser

// code readability is high must be


//// --->> premitive datatypes
let name = "Gourav";
console.log(typeof(name));  //string "" &&  ''

let age = 12;
console.log(typeof(age));  // number  bigint

let islog = false;
console.log(typeof(islog));  // bool true and false

//NULL standalone value    repsrent as empty value

// undefined  value doesn't defines

// symbol (react) unique



//object

console.log(typeof null);  // object
console.log(typeof undefined);  // undefined


const s = 100;
const scv = 100.4;

const islogg = false;
const outside = null;
let useremail;

const id = Symbol('123');
const andotjerif = Symbol('123')
/*

    Javascript Dynamic type

 primitive data types (call by value)
    7 types
    string,number,bool,null,undefined,symbola and BigInt

 non primitive data types(refernce types)
    array,objects,functions
*/

const  hero = ['hej','jeje','eje'];
let myobhj={
    n:"gsf",
    age:22,
}

const myfun = function(){
    
}